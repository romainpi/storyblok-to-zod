import { beforeEach } from 'vitest';

// Reset global state before each test
beforeEach(() => {
  // We'll need to add clear methods if we want to reset state
  // or work around this in our tests
});

// Export test utilities as constants
export const TEST_DATA_DIR = './tests/fixtures';
export const TEST_OUTPUT_DIR = './tests/output';